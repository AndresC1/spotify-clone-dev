import { ReactElement } from "react";

interface LinkBoxNavbarProps {
    ruta: string;
    name: string;
    iconDisabled: ReactElement;
    iconActived: ReactElement;
    id: number;
}

export default LinkBoxNavbarProps